# libcu132
libcu132 is a lightweight C library for communicating with the Carrera Digital Control Unit over its serial interface.

It provides a simple API for establishing a connection, polling data and parsing sensor and status updates.
The library is designed to remain small, portable and easy to bind from other languages for custom monitoring or management software.

libcu132 requires:
- libserialport